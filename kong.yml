_format_version: "2.1"
_transform: true

services:
  - name: my-service
    url: https://example.com
    plugins:
    - name: key-auth
      #service: transaction-service
      config:
        key_names:
        - apikey

    routes:
    - name: my-route
      paths:
      - /

consumers:
- username: user123
  custom_id: rf123456

  keyauth_credentials:
  - key: RF123456
  #- consumer: {consumer}


#plugins:
#- name: syslog
#  consumer: my-user
#  service: my-service

